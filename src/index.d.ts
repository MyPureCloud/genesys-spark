export * from './components';

// This must be kept in sync with `scripts/stencil-wrapper.ts` until we
// find a way use the types form that file directly.
export declare function registerElements(): void;
