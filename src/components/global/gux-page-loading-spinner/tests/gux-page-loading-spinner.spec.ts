import { GuxPageLoadingSpinner } from '../gux-page-loading-spinner';

describe('gux-page-loading-spinner', () => {
  it('builds', () => {
    expect(new GuxPageLoadingSpinner()).toBeTruthy();
  });
});
